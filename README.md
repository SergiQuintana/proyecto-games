# Proyecto Games — internal status board

**Private business communication between the owner and his business partner.**
Not a product page, not a marketing channel, not a distribution surface. Please do not
share these URLs publicly or link them from social media.

Auto-published from the pipeline: `index.html` is compiled by
`pipeline/render_dashboard.py` from the pipeline's data files and pushed by the daily
collector task. Do not edit it by hand — the next run overwrites it.

Games under `games/<slug>/` are work-in-progress builds included so the partner can try
them. Their status is shown on the board; some have not passed QA.
